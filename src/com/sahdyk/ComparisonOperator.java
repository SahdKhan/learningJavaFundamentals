package com.sahdyk;

// #15
public class ComparisonOperator {
    public static void main(String[] args){
        int x = 1;
        int y = 1;
        System.out.println(x == y);
        // This will output TRUE because x is equal to y

        /*
        Definitions:
        == (Equals)
        != (Does not equal to)
        > (Greater Than)
        >= (Greater than or equal to)
        < (Less than)
        <= (Less than or equal to)
         */
    }
}
